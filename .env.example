# Copy this file to .env and fill in your values.
# Never commit .env to version control.

# Razorpay API keys — get from dashboard.razorpay.com → Settings → API Keys
RAZORPAY_KEY_ID=rzp_test_TWrIP12OblJjUf
RAZORPAY_KEY_SECRET=NjBZE2sNnw3KJEBj6Re58x3H

# Shared secret for internal system endpoints (/deduct, /withdraw).
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# Production: replace with proper session/JWT auth.
INTERNAL_TOKEN=your_strong_random_token_here

# Set to 'true' only in local development — never in production.
FLASK_DEBUG=false
